const info = {          //объявляем объект 
    city: "Krasnodar",  //объявлем элемент city
    temperature: 5      //объявлем элемент temperature
};

console.log(info.city + ` ${info.temperature} градусов`);  //выводим на консоль информации из объекта

//

const result = (2 + 2) * 3;     //присваем result значение примера
console.log(result);            //выводим result в консоль